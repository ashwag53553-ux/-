اقرأ `prompts/_SAFE_HEADER.md` والتزم كل بند فيه، ثم طبّق قواعد `CLAUDE.md` الستّ الدائمة.

# مهمّة: تركيب مكينة الرسالة + جرد كامل لحالة المشروع (لا تكتب محتوى الرسالة الآن)

أنت تعمل داخل مجلّد العمل: `C:\Users\H\OneDrive\Desktop\شغل الرسالة كامل`

## القيود الصارمة
- ممنوع اختلاق أي رقم أو ملف أو مصدر. كل رقم في تقريرك ناتجُ أمرٍ نفّذته فعلاً.
- ممنوع تعديل أي ملف من ملفات الرسالة في هذه المهمّة. أنت تركّب وتجرد فقط.
- ممنوع إنشاء أي ملف وورد جديد. المستند الأمّ واحد: `outputs/docx/الرسالة_الكاملة.docx`.
- لا تسأل أسئلة قبل إنجاز الجرد كاملاً. اسأل في آخر التقرير فقط، وبثلاثة أسئلة كحدّ أقصى.

## الخطوة ١ — تركيب المكينة
حزمة المكينة مدفوعة إلى فرع `claude/thesis-architecture-setup-r90xlb` في المستودع
`ashwag53553-ux/-`. نفّذ داخل مجلّد العمل:
```bash
git status                       # تأكّد أن العمل نظيف؛ إن لا، احفظه بـ git stash
git fetch origin claude/thesis-architecture-setup-r90xlb
git checkout -b machine origin/claude/thesis-architecture-setup-r90xlb -- 2>NUL || \
  git merge --allow-unrelated-histories origin/claude/thesis-architecture-setup-r90xlb
```
إن تعارض الدمج: **أبقِ ملفات الرسالة كما هي** واقبل ملفات المكينة
(`CLAUDE.md`، `thesis.config.json`، `tools/`، `guards/`، `source_registry/`،
`evidence_bank/`، `state/`، `prompts/`، `.claude/skills/thesis-factory/`).
ثم:
1. إن وُجد `CLAUDE.md` سابق فيه قرارات خاصة: احفظه باسم `CLAUDE.local.md`
   وادمج قراراته في قسم «قرارات معتمدة» من الدستور الجديد.
2. اضبط في `thesis.config.json`: `library_root` و`clips_root` و`chapter_order`
   لتطابق مسارات جهازي الحقيقية.
3. نفّذ: `pip install python-docx pypdf`
4. نفّذ: `python tools/thesis.py status` وألصق مخرجه كما هو.

## الخطوة ٢ — جرد آليّ (نفّذ الأوامر، لا تُقدّر)
اجمع بالأرقام الحقيقية:
- عدد ملفات `.md` تحت `chapters/` وحجم كل منها بالكلمات.
- عدد ملفات التفريغ (`.srt`/`.txt`) تحت `chapters/chapter02/transcripts/` وأسماؤها.
- محتويات `outputs/` و`outputs/matalib/` و`outputs/docx/` (الأسماء والتواريخ والأحجام).
- هل يوجد `project_brief/02_thesis_plan.md`؟ لخّص هيكل الأبواب والفصول والمطالب منه
  كما هو مكتوباً، بلا زيادة.
- محتويات `guards/` و`tools/` و`source_registry/` و`evidence_bank/` السابقة إن وُجدت.
- في مكتبة المراجع: `python tools/index_library.py` (بلا `--write` أولاً) وألصق المخرج.

## الخطوة ٣ — فحص الوضع الحالي بالحُرّاس
شغّل على كل مسودّات الفصول الموجودة:
`python tools/thesis.py check chapters/**/*.md`
ثم لخّص: كم ملفاً اجتاز، وأكثر ثلاث مخالفات تكراراً، وكم سطراً فيه أرقام غربية.

## الخطوة ٤ — تعبئة لوحة الحالة
املأ `state/ledger.md` بالأرقام الحقيقية التي جمعتها (لا تقديرات)، والتزم بجدول الفصول.

## الخطوة ٥ — تقرير نهائي بهذا الشكل حرفياً
```
جرد المشروع
────────────
١) الفصول الموجودة: (قائمة بالمسار وعدد الكلمات وحالة الحُرّاس)
٢) المدوّنة المرئية: (عدد التفريغات، أسماء أبرزها، أي نواقص)
٣) المكتبة: (عدد PDF، المفهرس، الممسوح المحتاج إلى OCR)
٤) خطة الرسالة كما في project_brief: (الهيكل كما هو)
٥) أعطاب اكتُشفت: (ترتيبها بالخطورة، وسبب كلٍّ منها من جذره)
٦) ما ينقص لبدء أول مطلب: (بنود قابلة للتنفيذ)
٧) ثلاثة أسئلة لا يمكن حسمها إلا مني
```
ثم اختم بتقرير التحقّق المنصوص عليه في CLAUDE.md §٦.
