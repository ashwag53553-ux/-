# دستور الرسالة — مصدر الحقيقة الوحيد

> يُقرأ تلقائياً في كل جلسة. كل قرار جديد يُعتمد يُضاف هنا فيسري على كل الفصول.
> الاستثناء لفصلٍ بعينه لا يكون إلا مكتوباً في `chapters/<الفصل>/EXCEPTIONS.md`.

## ٠) هوية العمل

- **الرسالة:** ماجستير في العقيدة والمذاهب المعاصرة — جامعة الملك خالد.
- **العنوان:** «التفسير الطبيعي في الخطاب العلمي المرئي العربي بين الحضور والتوظيف
  في قضايا أصل الإنسان — دراسة تحليلية نقدية».
- **الأطروحة المركزية:** الفصل بين *العلم التجريبي* و*الطبيعانية الفلسفية*؛
  وأن التفسير الطبيعي يُقدَّم في الخطاب المرئي العربي بلبوس «العلم القاطع»؛
  والتمييز الحاسم بين **الحضور** (ورود التفسير الطبيعي عرضاً) و**التوظيف**
  (تسخيره لترسيخ نتيجة فلسفية إلحادية).
- **الأساس المعتمد للصوت والأسلوب:**
  `outputs/matalib/00_preliminary_chapter_REVIEW_AUTHENTIC_RESEARCHER_VOICE.md`
  — يُقرأ قبل كتابة أي مطلب جديد، ويُحتذى نبرةً وإيقاعاً.
- **المستند الأمّ الواحد:** `outputs/docx/الرسالة_الكاملة.docx` — لا نسخ متعددة،
  ولا ملف وورد ثانٍ بأي حال.

---

## ١) القواعد الستّ الدائمة (حوكمة على كل الفصول)

**١. RTL كامل.**
كل نص وفقرة وهامش وجدول ومرجع وعنوان باتجاه يمين→يسار، بما في ذلك اتجاه الخط
الفاصل للحواشي واتجاه الجداول (`bidiVisual`) وهوامش الصفحة (`rtlGutter`).
والأرقام **عربية شرقية ٠-٩** في كل سياق عربي، **ومنها أرقام الحواشي**.
> طبقة الأرقام مستقلة عن طبقة الاتجاه: الترقيم التلقائي في وورد يتبع إعداد الجهاز
> لا المستند، ولذلك يبني المحرّك علامات حواشٍ **مخصّصة** (`customMarkFollows`)
> نصُّها رقمٌ عربي شرقي — فتظهر ٠-٩ على أي جهاز، ويُعاد الترقيم في كل بناء.

**٢. صفر أثر آلة.**
لا علامة مائية، ولا رمز مخفي (ZWSP/ZWJ/LRM/RLM/BOM/بيدي)، ولا إيموجي، ولا
عبارة تكشف الآلية، ولا بيانات وورد كاشفة (المؤلف/المراجعات/rsid/آثار القوالب).

**٣. تفاوت أطوال الفقرات طبيعياً.**
لا رتابة إيقاعية: فقرة قصيرة حاسمة بعد فقرتين مطوّلتين، ومعامل اختلاف الأطوال
لا يقلّ عن ٠٫٣٠ في كل ملف، ولا تتوالى أكثر من فقرتين متقاربتي الطول.

**٤. الهوامش للكتب والمقالات والمراجع فقط.**
الحلقات والمقاطع تُذكر **مختصرة داخل المتن** (اسم المتحدث + عنوان الحلقة +
التوقيت)، ولا تنزل إلى الهامش أبداً.

**٥. تمييز حاسم بين الصفّين.**
- *من معنا:* أهل السنة والجماعة ومَن وافق الحق في المسألة (سامي عامري،
  عمرو شريف، هيثم طلعت، ومَن نقد الطبيعانية من فلاسفة العلم).
- *من ضدّنا:* داعمو التطور والطبيعانية (سلامة موسى، داروين، عدنان إبراهيم
  في مواضع مخالفته، وأي مؤلف خالف — ومنه «أبو آدم» حيث خالف).
- النقد قويّ مباشر بلا تذبذب ولا مجاملة، ومع ذلك أميناً في النقل عن المخالف.

**٦. صياغة أكاديمية بشرية ومراجع كاملة.**
كل إحالة: المؤلف، الكتاب، الجزء والصفحة، الطبعة، الدار، السنة. وملف وورد واحد.

---

## ٢) الصدق المطلق (فوق كل قاعدة)

- **يُمنع منعاً باتاً** اختلاق مصدر أو صفحة أو نصّ أو توقيت مقطع.
- ما لم يُفتح فعلاً ويُقرأ فعلاً **لا يُستشهد به**.
- كل ما لم يثبت يُنقل إلى **«صفحة التحقّق الحمراء»** في آخر الملف بعنوان بارز:
  `## للتحقّق قبل التسليم — تُحذف من النسخة النهائية`
  ويُعلَّم في مكانه بـ `[[تحقق]]`. ولا يُسلَّم البحث وفيها بند واحد.
- إن تعذّر إثبات شيء: قُلها صراحةً. لا تخمين، ولا «صفحة تقريبية».

---

## ٣) محكمة الإحالات (لا استثناء)

| الدعوى | شرط القبول |
|---|---|
| كل هامش | قيدٌ مطابق في `source_registry/registry.tsv` + صفحة داخل حدود صفحات الكتاب |
| كل شاهد | مسجَّل في `evidence_bank/quotes.tsv` بمصدر وصفحة ونصّ كامل |
| كل دعوى من مقطع | توقيت حقيقي في ملف SRT + تطابق لفظي ≥ ٥٠٪ مع التفريغ عند ذلك التوقيت |

`python guards/citation_court.py <files>` هي القاضي، ومخرجها يبني الصفحة الحمراء آلياً.

---

## ٤) معايير المحتوى (حدود دنيا لا سقوف)

لكل **مطلب** — لا لكل فصل:

1. **تنوّع الأدلة إلزامي** من ستّة مشارب: قرآن؛ سنة **بتخريج**؛ نقل عن السلف؛
   معاصرون ناقدون للتطور؛ فلسفة العلم؛ وكلام المخالفين أنفسهم من كتبهم.
   ويُضاف إليها مدوّنة المقاطع (التفريغات) بوصفها مادة الدراسة.
2. **حدّ أدنى: ٦ استشهادات حقيقية** من ٤ مصادر مختلفة على الأقل، بصفحات حقيقية،
   منها شاهدٌ واحد على الأقل من كلام المخالف نفسه.
3. **تحليل نقدي للباحثة في كل مطلب** — لا سرد اقتباسات: مقدّمة تُحرّر محلّ النزاع،
   ثم عرض أمين لقول المخالف، ثم نقده من جهاته (المنهجية/العقدية/المنطقية)،
   ثم خلاصة تربطه بالأطروحة المركزية.
4. **تدرّج التنسيق** بلا تكدّس: عنوان ← تمهيد ← عرض ← نقد ← خلاصة، وانتقالات
   لفظية بين المطالب لا قفزات.
5. **الاصطلاح منضبط:** «التفسير الطبيعي»، «الطبيعانية الفلسفية»، «الحضور»،
   «التوظيف» — تُستعمل بمعناها المحرَّر في التمهيد ولا تُخلط.

---

## ٥) خطّ الإنتاج (لا يُبنى فصلٌ لم يجتَز الحُرّاس)

```
مسودّة .md  →  الحُرّاس  →  محكمة الإحالات  →  بناء المستند الأمّ  →  تقرير تحقّق  →  فتح وورد
```

أمرٌ واحد يقود الخطّ كلّه:

```bash
python tools/thesis.py ship chapters/chapter02/*.md --open
python tools/thesis.py check <files>     # الحُرّاس فقط
python tools/thesis.py court <files>     # المحكمة + الصفحة الحمراء
python tools/thesis.py status            # لوحة الحالة
```

**قواعد كتابة المسودّة (لغة المصنع):**

- `#` باب/فصل، `##` مبحث، `###` مطلب، `####` فرع.
- الهامش: `[^١]` في المتن، وتعريفه في آخر الملف: `[^١]: المؤلف، *الكتاب*، الدار، المدينة، ط٢، ١٤٣٧هـ، ص ١٢٤.`
- الاقتباس المطوّل: سطر يبدأ بـ `>`.
- كسر صفحة: `<<<pagebreak>>>`.
- الأرقام تُكتب عربية شرقية من الأصل (والحُرّاس ترفض الغربية).
- المشكوك فيه: `[[تحقق]]` + بند في الصفحة الحمراء.

---

## ٦) تقرير التحقّق (يُختم به كل مخرج، بلا استثناء)

```
تقرير التحقّق
─────────────
• الاستشهادات: (عدد) من (عدد) كتاباً فُتح فعلاً — أسماؤها: …
• الشواهد الجديدة المضافة إلى بنك الشواهد: (عدد)
• دعاوى المقاطع الموثّقة بتوقيت SRT: (عدد)
• RTL: مقطع/فقرة/تشغيل/جدول/هامش/فاصل ✔
• الأرقام: عربية شرقية ٠-٩ ومنها أرقام الحواشي ✔
• الهوامش: كتب/مقالات فقط — الحلقات في المتن ✔
• أثر الآلة: رموز مخفية ٠ | عبارات نمطية ٠ | معامل تنوّع الأطوال (…)
• التصنيف: من معنا (…) / من ضدّنا (…)
• الصفحة الحمراء: (عدد) بنداً — تفصيلها: …
• لمراجعتك أنتِ: (سطر واحد بما يحتاج قراراً بشرياً)
```

---

## ٧) عقيدة التشغيل

- **قانون الجذر:** كل عطب يُصلَح في طبقته، ويُمنع تكراره بحارس آليّ،
  ويُقيَّد في `state/defects.md`. ما لم يُمنع آلياً فهو لم يُصلَح.
- **انضباط النطاق:** لا يُمسّ ملف خارج نطاق المهمة المعلن. لا حذف، ولا
  مساس بالحُرّاس أو الإعدادات أو الدستور، ولا وورد ثانٍ — إلا بإذن صريح.
  والتحقّق آليّ: `python guards/scope_check.py --allow "<أنماط المهمة>"`.
- **لا حدّ أدنى:** المخرج المقبول هو المُبهر، لا المُجزئ.
- **استباق:** اقترح الأداة والسكيل والنظام قبل أن يُطلب.
- **اقتصاد التوكنز:** لا تُعِد قراءة ما قُرئ؛ أعِد استخدام `evidence_bank/`؛
  اعمل في Markdown ثم ابنِ مرة واحدة؛ حدّث `state/ledger.md` لاستئناف نظيف.

---

## ١٠) لغة الخطاب مع الباحثة (قاعدة ثابتة)

الباحثة ليست مبرمجة، فكل ما يُكتب لها:

- **بسيط**: بلا مصطلحات تقنية، وإن لزم مصطلح شُرح في كلمتين بين قوسين.
- **قصير**: أقصر جواب يؤدّي الغرض. لا تكرار، ولا استعراض خيارات لن تُسلك.
- **عملي**: ماذا تفعل الآن بالضبط، في خطوة أو خطوتين.
- **صادق**: إن كان في الأمر خطر أو تكلفة قيلت صراحة قبل التنفيذ.

**وكل رسالة فيها عملٌ تصنعه الباحثة تُختم ببطاقة «خطواتك» — ثلاثة أفعال
لا أكثر، كلٌّ يبدأ بفعل أمر، ومعها الزمن المتوقَّع:**

```
خطواتك (٣٠ ثانية)
١) نزّلي المرفق — واتركيه في Downloads، لا تنقليه
٢) الصقي المربّع في كلود المحلي
٣) انتظري ✔ ثم أرسليها لي
```

وقاعدةٌ ثابتة تُعلَن مرة وتُعتمد دائماً: **الباحثة لا تنقل ملفاً ولا تفتح
مجلداً ولا تحرّر نصاً بيدها.** أفعالها ثلاثة فقط: تنزيل، ولصق، ونسخ النتيجة.

أمّا ما يُكتب للأداة (البرومتات) فصارم برمجيّ مفصّل — والفرق بين الخطابين مقصود.

---

## ٩) بروتوكول التوقّع (يسبق كل مهمة طويلة)

الباحثة ليست مبرمجة، ولا يصحّ أن تنتظر أمام شاشة لا تفهم ما يجري فيها.
فقبل أي عملية تتجاوز دقيقة أو تمسّ ملفات، تُكتب **بطاقة توقّع** من خمسة أسطر:

```
ماذا سيحدث الآن : (جملة واحدة بلا مصطلحات)
كم يستغرق      : (تقدير صادق، ويُقال «لا أعرف» إن جُهل)
علامة النجاح    : (السطر أو الرقم الذي يظهر فعلاً عند النجاح)
علامة الفشل     : (شكل الخطأ المتوقَّع وما يعنيه)
دورك أنتِ       : (انتظري / اضغطي موافقة / أرسلي المخرج)
```

وأثناء العمليات الطويلة يُعلَن التقدّم مرحلةً مرحلة، لا صمتٌ ثم نتيجة.
وبعد الانتهاء يُقال بلغة بسيطة: ما الذي تغيّر فعلاً، وأين أثره، وما التالي.

---

## ٨) بطاقة المشروع (أول ما يُقرأ في كل جلسة)

`state/SNAPSHOT.md` صفحةٌ واحدة تُبنى آلياً وتحوي: خريطة كل مسودّة وعناوينها
وحالتها عند الحُرّاس وبصمتها، والمدوّنة المرئية، والمكتبة، والبنوك، والمستند
الأمّ، والصفحة الحمراء، والقرارات المعتمدة، والخطوة التالية.

- **تُقرأ أولاً، قبل فتح أي ملف.** ما ليس فيها يُفتح ملفه عند الحاجة فقط.
- **تُعاد بناؤها** بـ `python tools/snapshot.py` بعد كل تعديل أو بناء،
  وتلقائياً عند بدء الجلسة عبر خطّاف `SessionStart`.
- **لا تُحرَّر يدوياً** — هي مرآة للمشروع لا مصدرٌ له. مصدر الحقيقة هذا الدستور.
- **حدّها**: إن تجاوزت ~١٠ ك.ب فاختصر القوائم، ولا تحذف أقسامها.
