# أمثلة توضيحية — ليست من مادة الرسالة

- `نموذج_مطلب.md` — نموذج لغة المصنع: عناوين، اقتباس، جدول، هوامش بأرقام عربية.
- `transcripts/نموذج.srt` — نموذج تفريغ لاختبار محكمة الإحالات.
- `bank_demo/*.demo.tsv` — نماذج تعبئة سجل المصادر وبنك المقاطع.

**تحذير:** بيانات هذه الملفات وهمية للاختبار فقط. لا تُنقل إلى
`source_registry/registry.tsv` ولا إلى `evidence_bank/` بحال. السجلّات الحقيقية
تُملأ من كتب فُتحت فعلاً، وإلا فالصفحة الحمراء.
